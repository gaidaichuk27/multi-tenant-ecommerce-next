export interface PasswordChangeFormData {
    oldPassword: string;
    newPassword: string;
    repeatNewPassword: string;
}

export interface PasswordChangeBody {
    oldPassword: string;
    newPassword: string;
}
